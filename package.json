{
  "name": "tron-workshop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "tronbox compile",
    "deploy:nile": "tronbox migrate --network nile",
    "demo:contract": "node scripts/contract-demo.js",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:start": "npm --prefix frontend run start",
    "send:trx": "node scripts/send-trx.js",
    "send:usdt": "node scripts/send-usdt.js"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "tronweb": "^6.2.2"
  },
  "devDependencies": {
    "tronbox": "^4.5.0"
  }
}
